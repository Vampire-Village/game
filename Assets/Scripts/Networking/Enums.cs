namespace VampireVillage.Network
{
    public enum NetworkMode
    {
        Offline,
        Client,
        Server
    }

    public enum RoomState
    {
        Lobby,
        Game
    }
}
